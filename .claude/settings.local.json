{
  "permissions": {
    "allow": [
      "Bash(for dir in sample_data/invalid/*/)",
      "Bash(do mkdir -p \"$dir/localization\")",
      "Bash(done)",
      "Bash(cmake -B build -DCMAKE_BUILD_TYPE=Debug)"
    ]
  }
}
